import React, { Component } from 'react';

class ContactProfile extends Component {
  render() {
    const { profile } = this.props;

    return (
      <div>Contact profile will be toggled on and off</div>
    );
  }
}